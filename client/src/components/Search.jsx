import React from 'react';

const Search = () => (
  <div className="container search">
    <input className="input" type="text" placeholder="City, state, zip code..." />
    <button className="button is-dark">Submit</button>
  </div>
);

export default Search;
